# Database (Plesk MariaDB — ajusta prefijo según tu panel)
# Formato: mysql://USUARIO:CONTRASEÑA@localhost:3306/NOMBRE_BD
DATABASE_URL="mysql://joseisl1_jipcoaching_admin:TU_CONTRASEÑA_GENERADA@localhost:3306/joseisl1_jipcoaching"

# JWT (CAMBIA ESTO — usa una cadena larga y aleatoria)
JWT_SECRET="genera-una-clave-secreta-de-al-menos-32-caracteres"
JWT_EXPIRES_IN="7d"

# Server
PORT=3001
NODE_ENV=production

# CORS (dominio del frontend)
CORS_ORIGIN="https://jipcoaching.com"

# Upload paths (relative to backend root)
UPLOAD_DIR="./uploads"
